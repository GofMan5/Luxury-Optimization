{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "storage-analyzer-window",
  "description": "The standalone storage analyzer can use the shared sidecar and hide its own window.",
  "windows": ["storage-analyzer"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
